---

- hosts: all
  become: true
  tasks:

  - name: Install httpd and php packages
    dnf:
      name: 
        - httpd
        - php
      state: latest
    when: ansible_distribution in ["Rocky", "CentOS"]
